import { FC } from 'react';
import { Container } from '@mui/material';

const AdminPage: FC = () => {
  return (
    <Container maxWidth="md" sx={{ pt: 3 }}>
      Admin page
    </Container>
  );
};

export { AdminPage };
